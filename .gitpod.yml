tasks:
  - init: |
      docker-compose pull
  - command: |
      docker-compose up

ports:
  - port: 5432
    onOpen: ignore
  - port: 8081
    onOpen: open-browser

vscode:
  extensions:
    - ms-azuretools.vscode-docker